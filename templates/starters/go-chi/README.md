# {{PROJECT_NAME}}

Generated by the Ironflyer scaffold on {{TODAY}}.

## Stack

- Go 1.22+
- github.com/go-chi/chi v5

## Structure

```
main.go     HTTP server with /health and / endpoints
go.mod      Go module (chi dependency)
```

## Run

```bash
go mod tidy
go run .
```

Server listens on `:8080` (override with `PORT`).

- `GET /`        — Welcome HTML
- `GET /health`  — JSON health check
