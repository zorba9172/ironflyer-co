@rem
@rem Minimal Gradle wrapper bootstrap (Windows).
@rem
@rem The full wrapper (gradle-wrapper.jar + scripts) is intentionally not
@rem committed here. To materialise it, run on any host with Gradle installed:
@rem
@rem   gradle wrapper --gradle-version 8.11.1
@rem
@rem Android Studio will also offer to regenerate the wrapper when the project
@rem is opened for the first time.

@echo off
echo gradlew is not initialised yet.
echo Run `gradle wrapper --gradle-version 8.11.1` from a host with Gradle installed,
echo or open this project in Android Studio so it can regenerate the wrapper.
exit /b 1
