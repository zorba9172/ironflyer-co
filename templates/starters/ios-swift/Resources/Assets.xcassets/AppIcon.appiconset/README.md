# App icon

Drop a 1024x1024 PNG here named `icon-1024.png`. Xcode will derive every
required size automatically (iOS 14+ single-size app icon catalog).

The file is intentionally not committed so the starter stays text-only.
Until you add it, the app builds but ships with the system placeholder
icon.
