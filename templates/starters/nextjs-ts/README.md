# {{PROJECT_NAME}}

Generated by the Ironflyer scaffold on {{TODAY}}.

## Stack

- Next.js 15 (App Router)
- TypeScript (strict)
- MUI 6 + Emotion
- React 18

## Structure

```
app/
  layout.tsx          Root layout, dark Ironflyer theme
  page.tsx            Home page with a live /api/hello fetch
  api/hello/route.ts  Edge route, returns JSON
package.json          dev / build / start scripts
tsconfig.json         Strict TS, "@/*" path alias
next.config.js        React strict mode
```

## Run

```bash
npm install
npm run dev
```

Dev server boots on port 3000. The page polls `/api/hello` to prove the
end-to-end wire is alive — when the Ironflyer Coder agent edits this app,
the dev server's HMR will reflect changes instantly.
