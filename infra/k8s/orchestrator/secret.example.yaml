# Template only — DO NOT commit real values. Copy to orchestrator-secret.yaml,
# fill in, and apply with `kubectl apply -f orchestrator-secret.yaml` BEFORE
# the Deployment so it starts cleanly. Use sealed-secrets / external-secrets
# in real environments.
apiVersion: v1
kind: Secret
metadata:
  name: orchestrator-secrets
  namespace: ironflyer
type: Opaque
stringData:
  IRONFLYER_JWT_SECRET: "change-me-to-a-32-byte-random-string"
  ANTHROPIC_API_KEY: ""
  OPENAI_API_KEY: ""
  GITHUB_CLIENT_ID: ""
  GITHUB_CLIENT_SECRET: ""
  STRIPE_SECRET_KEY: ""
  STRIPE_WEBHOOK_SECRET: ""
  STRIPE_PRICE_PRO: ""
  STRIPE_PRICE_TEAM: ""
  STRIPE_PRICE_ENTERPRISE: ""
