# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
!clients/web/next-env.d.ts

# Compiled SDK / package build outputs
packages/*/dist/

# Test artifacts
clients/web/test-results/
clients/web/screenshots/
clients/web/tmp-screenshots/
playwright-report/
coverage/

# Local template scratchpad — third-party template packages staged for
# inspection. NEVER commit; the repo curates a small subset under
# clients/web/public/templates/ instead.
/templates/

# Go
bin/
vendor/
*.exe
*.test
*.out
# Compiled Go service binaries (built next to main.go) — never commit.
core/orchestrator/orchestrator
core/runtime/runtime
core/cli/cli

# Third-party marketing asset packs (PSDs, MP4s, MOGRTs, huge PNGs) —
# kept locally for design reference but not tracked.
assets/market/

# Env / secrets
.env
.env.local
.env.*.local
.ironflyer/ide-sync/

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
