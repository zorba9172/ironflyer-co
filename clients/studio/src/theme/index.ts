// Studio Neon Intelligence theme — public surface.
// Components import mode-aware colors via `theme.palette.*` and neon brand
// marks via `theme.studio.*` (or `studioTokens` for non-sx contexts).
export { studioTheme, type StudioExtras } from './studioTheme';
export { StudioThemeProvider, useThemeMode } from './StudioThemeProvider';
export { studioTokens, neon, gradient, modes, effect, chart, radius, motion, font, type StudioMode } from './tokens';
